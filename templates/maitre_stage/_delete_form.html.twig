<form method="post" action="{{ path('maitre_stage_delete', {'id': maitre_stage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maitre_stage.id) }}">
    <button class="btn">Delete</button>
</form>
